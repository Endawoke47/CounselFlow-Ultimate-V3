import { UpdateMatter } from '@/features/manage-matter/ui';
import Layout from '@/widgets/layout/Layout';

export const UpdateMatterPage = () => {
  return (
    <Layout>
      <UpdateMatter />
    </Layout>
  );
}
import { CreateMatter } from '@/features/manage-matter/ui';
import Layout from '@/widgets/layout/Layout';

export const AddMatterPage = () => {
  return (
    <Layout>
      <CreateMatter />
    </Layout>
  );
}
import { CreateCompany } from '@/features/manage-companies/ui';
import Layout from '@/widgets/layout/Layout';

export const AddCompanyPage = () => {
  return (
    <Layout>
      <CreateCompany />
    </Layout>
  );
}

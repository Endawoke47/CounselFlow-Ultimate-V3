import { CompaniesContent } from '@/widgets/companies-view/ui';
import Layout from '@/widgets/layout/Layout';

export const CompaniesPage = () => {
  return (
    <Layout>
      <CompaniesContent />
    </Layout>
  );
};

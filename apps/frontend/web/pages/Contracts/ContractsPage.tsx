import { ContractsContent } from '@/widgets/contracts-view/ui';
import Layout from '@/widgets/layout/Layout';

export const ContractsPage = () => {
  return (
    <Layout>
      <ContractsContent />
    </Layout>
  );
};


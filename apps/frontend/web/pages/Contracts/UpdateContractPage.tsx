import { UpdateContract } from '@/features/manage-contracts/ui';
import Layout from '@/widgets/layout/Layout';

export const UpdateContractPage = () => {
  return (
    <Layout>
      <UpdateContract />
    </Layout>
  );
}
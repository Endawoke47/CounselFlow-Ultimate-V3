import { CreateContract } from '@/features/manage-contracts/ui';
import Layout from '@/widgets/layout/Layout';

export const AddContractPage = () => {
  return (
    <Layout>
      <CreateContract />
    </Layout>
  );
}
import { UpdateAction } from '@/features/manage-actions/ui';
import Layout from '@/widgets/layout/Layout';

export const UpdateActionPage = () => {
  return (
    <Layout>
      <UpdateAction />
    </Layout>
  );
}

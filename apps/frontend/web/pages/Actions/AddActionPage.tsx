import { CreateAction } from '@/features/manage-actions/ui';
import Layout from '@/widgets/layout/Layout';

export const AddActionPage = () => {
  return (
    <Layout>
      <CreateAction />
    </Layout>
  );
}

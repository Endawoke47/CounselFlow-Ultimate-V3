import Layout from '@/widgets/layout/Layout';
import { UpdateDispute } from '@/features/manage-disputes/ui';

export const UpdateDisputePage = () => {
  return (
    <Layout>
      <UpdateDispute />
    </Layout>
  );
}

import Layout from '@/widgets/layout/Layout';
import { CreateDispute } from '@/features/manage-disputes/ui';

export const AddDisputePage = () => {
  return (
    <Layout>
      <CreateDispute />
    </Layout>
  );
}
